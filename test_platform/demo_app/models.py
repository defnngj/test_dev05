from django.db import models

# Create your models here. 操作数据库用的，ORM 对象关系映射

# python --> ORM--> 驱动pymysql/mysqldb ->  mysql
# sql= 'select * from table where id=1 and name="tom"'
# aa = exec (sql)
#
# ls = table.objects.all(pk=1, name="tom")
